"use client";

import React, { useState } from "react";
import ExpensesSection from "../sections/expenses";

export default function ExpensesPage() {
  return <ExpensesSection tenantSlug="default" />;
}
