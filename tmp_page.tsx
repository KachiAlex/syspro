"use client";

import React from "react";

export default function TenantAdminPage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-2xl font-semibold">Tenant Admin (temporarily disabled)</h1>
        <p className="text-sm text-slate-500 mt-2">The full tenant admin UI is disabled while we fix TypeScript errors.</p>
      </div>
    </div>
  );
}
